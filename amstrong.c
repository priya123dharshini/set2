#inclide <stdio.h>
int main ()
{
int nu,or,re,re=0;
printf ("enter a three digit integer:");
scanf("%d",&number);
or=nu;
while(or:=0)
{
remainder =or%10;
result +=remaider*remainder*remaider;
or/=10;
}
if(result==nu)
print("%d is an armstrong nu.", nu);
else
printf("%d is not an armstrong nu.",nu);
}
return 0;
